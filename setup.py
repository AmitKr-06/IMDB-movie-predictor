from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A machine learning pipeline to clean the IMDB 5000 movie dataset and predict movie success (IMDB rating/gross revenue) based on features like cast, budget, genre, and director.',
    author='Amit Kumar',
    license='',
)
